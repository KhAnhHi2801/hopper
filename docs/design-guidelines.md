# Design Guidelines

No UI exists yet — this project is backend-only through Phase 9. Frontend work (Vite Module Federation: `shell` host + `catalog-mfe` + `job-status-mfe` remotes) starts at Phase 10.

This file will be filled in once Phase 10-13 establish the frontend workspace, shared design system decisions (if any), and component conventions. See `plans/260728-2056-pod-to-shopify-import-system/plan.md` for the current frontend plan and open decisions (e.g. whether a shared UI library is used across the 3 apps).
